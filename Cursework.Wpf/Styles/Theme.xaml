<?xml version="1.0" encoding="utf-8"?>
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:sys="clr-namespace:System;assembly=mscorlib">

    <Color x:Key="Clr.Background">#FFF7F8FA</Color>
    <Color x:Key="Clr.Surface">#FFFFFFFF</Color>
    <Color x:Key="Clr.SurfaceAlt">#FFF3F4F7</Color>
    <Color x:Key="Clr.Border">#1A2B3340</Color>
    <Color x:Key="Clr.Primary">#FF2D7EF7</Color>
    <Color x:Key="Clr.PrimaryHover">#FF1F6AE0</Color>
    <Color x:Key="Clr.Accent">#FF18B892</Color>
    <Color x:Key="Clr.Danger">#FFE54B4B</Color>
    <Color x:Key="Clr.Warning">#FFF0A202</Color>
    <Color x:Key="Clr.Success">#FF2BB673</Color>
    <Color x:Key="Clr.Text">#FF101319</Color>
    <Color x:Key="Clr.TextMuted">#99101319</Color>
    <Color x:Key="Clr.Placeholder">#66101319</Color>
    <Color x:Key="Clr.Badge">#0F2D7EF7</Color>


    <!-- ===== Status Colors ===== -->
    <Color x:Key="Clr.Status.Free">#FF2BB673</Color>
    <Color x:Key="Clr.Status.Occupied">#FFE54B4B</Color>
    <Color x:Key="Clr.Status.Reserved">#FFF0A202</Color>
    <Color x:Key="Clr.Status.Cleaning">#FF2D7EF7</Color>
    <Color x:Key="Clr.Status.OutOfService">#FFB0B7C3</Color>

    <!-- Основные кисти статусов -->
    <SolidColorBrush x:Key="Brush.Status.Free" Color="{StaticResource Clr.Status.Free}"/>
    <SolidColorBrush x:Key="Brush.Status.Occupied" Color="{StaticResource Clr.Status.Occupied}"/>
    <SolidColorBrush x:Key="Brush.Status.Reserved" Color="{StaticResource Clr.Status.Reserved}"/>
    <SolidColorBrush x:Key="Brush.Status.Cleaning" Color="{StaticResource Clr.Status.Cleaning}"/>
    <SolidColorBrush x:Key="Brush.Status.OutOfService" Color="{StaticResource Clr.Status.OutOfService}"/>

    <!-- Полупрозрачные фоны для бейджей -->
    <SolidColorBrush x:Key="Brush.StatusBg.Free" Color="#112BB673"/>
    <SolidColorBrush x:Key="Brush.StatusBg.Occupied" Color="#11E54B4B"/>
    <SolidColorBrush x:Key="Brush.StatusBg.Reserved" Color="#11F0A202"/>
    <SolidColorBrush x:Key="Brush.StatusBg.Cleaning" Color="#112D7EF7"/>
    <SolidColorBrush x:Key="Brush.StatusBg.OutOfService" Color="#11B0B7C3"/>

    <!-- Базовый стиль бейджа -->
    <Style x:Key="BadgeBorder" TargetType="Border">
        <Setter Property="CornerRadius" Value="10"/>
        <Setter Property="Padding" Value="8,2"/>
        <Setter Property="Background" Value="{StaticResource Brush.Badge}"/>
    </Style>

    <!-- Pill-стили статусы -->
    <Style x:Key="StatusPill.Free" TargetType="Border" BasedOn="{StaticResource BadgeBorder}">
        <Setter Property="Background" Value="{StaticResource Brush.StatusBg.Free}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Status.Free}"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>
    <Style x:Key="StatusPill.Occupied" TargetType="Border" BasedOn="{StaticResource BadgeBorder}">
        <Setter Property="Background" Value="{StaticResource Brush.StatusBg.Occupied}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Status.Occupied}"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>
    <Style x:Key="StatusPill.Reserved" TargetType="Border" BasedOn="{StaticResource BadgeBorder}">
        <Setter Property="Background" Value="{StaticResource Brush.StatusBg.Reserved}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Status.Reserved}"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>
    <Style x:Key="StatusPill.Cleaning" TargetType="Border" BasedOn="{StaticResource BadgeBorder}">
        <Setter Property="Background" Value="{StaticResource Brush.StatusBg.Cleaning}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Status.Cleaning}"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>
    <Style x:Key="StatusPill.OutOfService" TargetType="Border" BasedOn="{StaticResource BadgeBorder}">
        <Setter Property="Background" Value="{StaticResource Brush.StatusBg.OutOfService}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Status.OutOfService}"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>


    <!-- Кисти -->
    <SolidColorBrush x:Key="Brush.Background" Color="{StaticResource Clr.Background}"/>
    <SolidColorBrush x:Key="Brush.Surface" Color="{StaticResource Clr.Surface}"/>
    <SolidColorBrush x:Key="Brush.SurfaceAlt" Color="{StaticResource Clr.SurfaceAlt}"/>
    <SolidColorBrush x:Key="Brush.Border" Color="{StaticResource Clr.Border}"/>
    <SolidColorBrush x:Key="Brush.Primary" Color="{StaticResource Clr.Primary}"/>
    <SolidColorBrush x:Key="Brush.PrimaryHover" Color="{StaticResource Clr.PrimaryHover}"/>
    <SolidColorBrush x:Key="Brush.Accent" Color="{StaticResource Clr.Accent}"/>
    <SolidColorBrush x:Key="Brush.Danger" Color="{StaticResource Clr.Danger}"/>
    <SolidColorBrush x:Key="Brush.Warning" Color="{StaticResource Clr.Warning}"/>
    <SolidColorBrush x:Key="Brush.Success" Color="{StaticResource Clr.Success}"/>
    <SolidColorBrush x:Key="Brush.Text" Color="{StaticResource Clr.Text}"/>
    <SolidColorBrush x:Key="Brush.TextMuted" Color="{StaticResource Clr.TextMuted}"/>
    <SolidColorBrush x:Key="Brush.Placeholder" Color="{StaticResource Clr.Placeholder}"/>
    <SolidColorBrush x:Key="Brush.Badge" Color="{StaticResource Clr.Badge}"/>

    <CornerRadius x:Key="Radius.M">12</CornerRadius>
    <CornerRadius x:Key="Radius.L">16</CornerRadius>
    <Thickness   x:Key="Pad.S">6</Thickness>
    <Thickness   x:Key="Pad.M">10</Thickness>
    <Thickness   x:Key="Pad.L">16</Thickness>

    <!-- ========= Общие тексты ========= -->
    <FontFamily x:Key="Font.UI">Segoe UI</FontFamily>
    <Style TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource Brush.Text}"/>
        <Setter Property="FontFamily" Value="{StaticResource Font.UI}"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
    </Style>
    <Style x:Key="H1" TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
        <Setter Property="FontSize" Value="22"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
    </Style>
    <Style x:Key="H2" TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
        <Setter Property="FontSize" Value="18"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
    </Style>
    <Style x:Key="H3" TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
    </Style>
    <Style x:Key="Caption" TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Foreground" Value="{StaticResource Brush.TextMuted}"/>
    </Style>

    <!-- ========= Карточка ========= -->
    <Style x:Key="Card" TargetType="Border">
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="CornerRadius" Value="{StaticResource Radius.L}"/>
        <Setter Property="Background" Value="{StaticResource Brush.Surface}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Border}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="{StaticResource Pad.L}"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect BlurRadius="12" ShadowDepth="0" Opacity="0.12"/>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ========= Кнопки ========= -->
    <Style x:Key="Button.Base" TargetType="Button">
        <Setter Property="FontFamily" Value="{StaticResource Font.UI}"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Background" Value="{StaticResource Brush.Primary}"/>
        <Setter Property="Padding" Value="10,8"/>
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Primary}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="bd"
                  CornerRadius="{StaticResource Radius.M}"
                  Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="6,2"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="{StaticResource Brush.PrimaryHover}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5"/>
                            <Setter Property="Cursor" Value="Arrow"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="Button.Outline" TargetType="Button" BasedOn="{StaticResource Button.Base}">
        <Setter Property="Background" Value="{StaticResource Brush.Surface}"/>
        <Setter Property="Foreground" Value="{StaticResource Brush.Text}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Border}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="bd"
                        CornerRadius="{StaticResource Radius.M}"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                        <ContentPresenter HorizontalAlignment="Center"
                                      VerticalAlignment="Center"
                                      Margin="6,2"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="BorderBrush" Value="{StaticResource Brush.Primary}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5"/>
                            <Setter Property="Cursor" Value="Arrow"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


    <Style x:Key="Button.Danger" TargetType="Button" BasedOn="{StaticResource Button.Base}">
        <Setter Property="Background" Value="{StaticResource Brush.Danger}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Danger}"/>
    </Style>

    <!-- ========= TextBox ========= -->
    <Style x:Key="TextBox.Base" TargetType="TextBox">
        <Setter Property="FontFamily" Value="{StaticResource Font.UI}"/>
        <Setter Property="Padding" Value="10"/>
        <Setter Property="MinHeight" Value="36"/>
        <Setter Property="Background" Value="{StaticResource Brush.Surface}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Border}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Foreground" Value="{StaticResource Brush.Text}"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border x:Name="Bd"
                        CornerRadius="{StaticResource Radius.M}"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                        <ScrollViewer x:Name="PART_ContentHost" Margin="8,6,8,6"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource Brush.Primary}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ========= PasswordBox ========= -->
    <Style x:Key="PasswordBox.Base" TargetType="PasswordBox">
        <Setter Property="FontFamily" Value="{StaticResource Font.UI}"/>
        <Setter Property="Padding" Value="10"/>
        <Setter Property="Background" Value="{StaticResource Brush.Surface}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Border}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Foreground" Value="{StaticResource Brush.Text}"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="PasswordBox">
                    <Border x:Name="Bd"
                        CornerRadius="{StaticResource Radius.M}"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                        <ScrollViewer x:Name="PART_ContentHost" Margin="8,6,8,6"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource Brush.Primary}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.6"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


    <!-- ========= ComboBox ========= -->
    <Style x:Key="ComboBox.Base" TargetType="ComboBox">
        <Setter Property="FontFamily" Value="{StaticResource Font.UI}"/>
        <Setter Property="Foreground" Value="{StaticResource Brush.Text}"/>
        <Setter Property="Background" Value="{StaticResource Brush.Surface}"/>
        <Setter Property="Padding" Value="10"/>
        <Setter Property="MinHeight" Value="36"/>
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Border}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="ScrollViewer.CanContentScroll" Value="True"/>
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <Border x:Name="Bd"
                            CornerRadius="{StaticResource Radius.M}"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"/>

                        <ToggleButton x:Name="Toggle"
                                  Background="Transparent"
                                  BorderThickness="0"
                                  Focusable="False"
                                  HorizontalAlignment="Stretch"
                                  VerticalAlignment="Stretch"
                                  IsChecked="{Binding IsDropDownOpen,
                                                      RelativeSource={RelativeSource TemplatedParent},
                                                      Mode=TwoWay}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <ContentPresenter Grid.Column="0"
                                              Margin="8,6,4,6"
                                              VerticalAlignment="Center"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"/>

                                <Path Grid.Column="1"
                                  Margin="0,0,10,0"
                                  Data="M 0 0 L 4 4 L 8 0 Z"
                                  StrokeThickness="1.5"
                                  Stroke="{StaticResource Brush.TextMuted}"
                                  HorizontalAlignment="Right"
                                  VerticalAlignment="Center"/>
                            </Grid>

                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Border Background="Transparent"
                                        CornerRadius="{StaticResource Radius.M}">
                                        <ContentPresenter HorizontalAlignment="Stretch"
                                                      VerticalAlignment="Stretch"/>
                                    </Border>
                                </ControlTemplate>
                            </ToggleButton.Template>
                        </ToggleButton>

                        <Popup x:Name="PART_Popup"
                           Placement="Bottom"
                           AllowsTransparency="True"
                           Focusable="False"
                           IsOpen="{TemplateBinding IsDropDownOpen}"
                           PopupAnimation="Slide">
                            <Border Margin="0,4,0,0"
                                CornerRadius="{StaticResource Radius.M}"
                                Background="{StaticResource Brush.Surface}"
                                BorderBrush="{StaticResource Brush.Border}"
                                BorderThickness="1"
                                Width="{Binding ActualWidth,
                                                RelativeSource={RelativeSource TemplatedParent}}">
                                <ScrollViewer>
                                    <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained"/>
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource Brush.Primary}"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource Brush.PrimaryHover}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.6"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>



    <!-- ========= ListView / GridView ========= -->
    <Style TargetType="ListView">
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Border}"/>
        <Setter Property="Background" Value="{StaticResource Brush.Surface}"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
    </Style>
    <Style TargetType="GridViewColumnHeader">
        <Setter Property="Foreground" Value="{StaticResource Brush.Text}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Padding" Value="8,6"/>
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Border}"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
        <Setter Property="Background" Value="{StaticResource Brush.SurfaceAlt}"/>
    </Style>

    <!-- ========= TabControl ========= -->
    <Style TargetType="TabControl">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabControl">
                    <DockPanel>
                        <TabPanel DockPanel.Dock="Top" Background="Transparent" Margin="0,0,0,8" IsItemsHost="True"/>
                        <Border Background="Transparent" BorderThickness="0">
                            <ContentPresenter ContentSource="SelectedContent"/>
                        </Border>
                    </DockPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="TabItem">
        <Setter Property="Padding" Value="14,8"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="{StaticResource Brush.Text}"/>
        <Setter Property="Panel.ZIndex" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Border x:Name="Bd"
                        Margin="0,0,8,4"
                        CornerRadius="10"
                        Background="{StaticResource Brush.SurfaceAlt}"
                        Padding="{TemplateBinding Padding}">
                        <ContentPresenter ContentSource="Header"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Bd" Property="Background"
                                Value="{StaticResource Brush.Surface}"/>
                            <Setter TargetName="Bd" Property="BorderBrush"
                                Value="{StaticResource Brush.Primary}"/>
                            <Setter TargetName="Bd" Property="BorderThickness" Value="1"/>

                            <Setter Property="Panel.ZIndex" Value="10"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


    <!-- ========= ScrollBar / Thumb ========= -->
    <Style TargetType="ScrollBar">
        <Setter Property="Width" Value="10"/>
        <Setter Property="Background" Value="Transparent"/>
    </Style>

    <Style TargetType="Thumb">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Thumb">
                    <Border Background="#33000000" CornerRadius="4"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ========= Hall: плитка столов ========= -->
    <Style x:Key="TableCardBorderStyle"
       TargetType="Border"
       BasedOn="{StaticResource Card}">
        <Setter Property="Effect" Value="{x:Null}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="8"/>
        <Setter Property="MinWidth" Value="200"/>

        <Setter Property="CacheMode">
            <Setter.Value>
                <BitmapCache/>
            </Setter.Value>
        </Setter>

        <Style.Triggers>
            <DataTrigger Binding="{Binding IsMouseOver,
                                      RelativeSource={RelativeSource AncestorType=ListBoxItem}}"
                     Value="True">
                <Setter Property="Background" Value="{StaticResource Brush.SurfaceAlt}"/>
            </DataTrigger>

            <DataTrigger Binding="{Binding IsSelected,
                                      RelativeSource={RelativeSource AncestorType=ListBoxItem}}"
                     Value="True">
                <Setter Property="BorderBrush" Value="{StaticResource Brush.Primary}"/>
                <Setter Property="BorderThickness" Value="2"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="TileItemContainerStyle" TargetType="ListBoxItem">
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Margin" Value="0"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="VerticalContentAlignment" Value="Top"/>
        <Setter Property="Focusable" Value="True"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <ContentPresenter/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="IngredientCard" TargetType="Border" BasedOn="{StaticResource Card}">
        <Setter Property="Padding" Value="12"/>
        <Setter Property="Margin" Value="0,0,0,8"/>
    </Style>

    <Style x:Key="FieldLabel" TargetType="TextBlock" BasedOn="{StaticResource Caption}">
        <Setter Property="Margin" Value="0,0,0,4"/>
    </Style>



    <!-- =========================
     Hall map context menu
     ========================= -->
    <Style x:Key="ContextMenu.Modern" TargetType="ContextMenu">
        <Setter Property="OverridesDefaultStyle" Value="True"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ContextMenu">
                    <Border Background="{StaticResource Brush.Surface}"
                        BorderBrush="{StaticResource Brush.Border}"
                        BorderThickness="1"
                        CornerRadius="12"
                        Padding="6"
                        SnapsToDevicePixels="True">
                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="MenuItem.Modern" TargetType="MenuItem">
        <Setter Property="Foreground" Value="{StaticResource Brush.Text}"/>
        <Setter Property="Padding" Value="10,8"/>
        <Setter Property="Margin" Value="2"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="MenuItem">
                    <Grid SnapsToDevicePixels="True">
                        <!-- Основной ряд -->
                        <Border x:Name="Bd"
                            Background="{TemplateBinding Background}"
                            CornerRadius="10"
                            Padding="{TemplateBinding Padding}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <!-- стрелка для sub-menu -->
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <ContentPresenter ContentSource="Icon"
                                              Margin="0,0,8,0"
                                              VerticalAlignment="Center"/>

                                <ContentPresenter Grid.Column="1"
                                              ContentSource="Header"
                                              RecognizesAccessKey="True"
                                              VerticalAlignment="Center"/>

                                <!-- Стрелка появляется только если есть подпункты -->
                                <TextBlock x:Name="SubArrow"
                                       Grid.Column="2"
                                       Text="›"
                                       Margin="12,0,0,0"
                                       VerticalAlignment="Center"
                                       Foreground="{StaticResource Brush.TextMuted}"
                                       Visibility="Collapsed"/>
                            </Grid>
                        </Border>

                        <!-- Popup для подменю -->
                        <Popup x:Name="SubMenuPopup"
                           Placement="Right"
                           PlacementTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}"
                           IsOpen="{TemplateBinding IsSubmenuOpen}"
                           AllowsTransparency="True"
                           Focusable="False"
                           PopupAnimation="Fade">
                            <Border Background="{StaticResource Brush.Surface}"
                                BorderBrush="{StaticResource Brush.Border}"
                                BorderThickness="1"
                                CornerRadius="14"
                                Padding="6">
                                <StackPanel IsItemsHost="True"
                                        KeyboardNavigation.DirectionalNavigation="Cycle"/>
                            </Border>
                        </Popup>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <!-- hover -->
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource Brush.SurfaceAlt}"/>
                        </Trigger>

                        <!-- disabled -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.55"/>
                        </Trigger>

                        <!-- Показать стрелку, если есть подпункты -->
                        <Trigger Property="HasItems" Value="True">
                            <Setter TargetName="SubArrow" Property="Visibility" Value="Visible"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


    <Style x:Key="MenuItem.Danger" TargetType="MenuItem" BasedOn="{StaticResource MenuItem.Modern}">
        <Setter Property="Foreground" Value="{StaticResource Brush.Danger}"/>
        <Style.Triggers>
            <Trigger Property="IsHighlighted" Value="True">
                <Setter Property="Background" Value="#1AE54B4B"/>
            </Trigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>